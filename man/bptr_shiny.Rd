% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tables.R
\name{bptr_shiny}
\alias{bptr_shiny}
\title{Launch the Interactive Shiny Explorer for BPTR Models}
\usage{
bptr_shiny(...)
}
\arguments{
\item{...}{Additional arguments passed to \code{shiny::runApp()}, such as
\code{port} or \code{launch.browser}.}
}
\value{
Launches a Shiny app in the default browser; does not return a value
  to the R session.
}
\description{
Opens the full interactive Shiny application bundled with the package
(\code{inst/app/app.R}).  The app supports data upload, model specification,
sequential testing, regime visualisation, bootstrap inference, and XLSX /
LaTeX export.
}
\details{
The application is organised into seven tabs:
\itemize{
  \item \strong{Data Preview} — dataset browser, descriptive statistics, and
    panel-mean time series.
  \item \strong{Model Results} — coefficient table (with style selection),
    forest plot, and model summary.
  \item \strong{Sequential Test} — F1,2 and F2,3 bootstrap test output with
    null-distribution plot and automatic regime-number decision.
  \item \strong{Regime Analysis} — threshold-variable scatter coloured by
    regime, dependent-vs-threshold scatter, and regime composition table
    (id, time, dep. variable, threshold variable, fitted values, residuals).
  \item \strong{Bootstrap CI} — bootstrap distribution of threshold
    parameter(s) and 95\% percentile confidence intervals.
  \item \strong{Model Comparison} — side-by-side PTR vs BTPD fit statistics
    (SSR, Within R\eqn{^2}, AIC, BIC).
  \item \strong{Diagnostics} — residual plots: vs fitted, Q-Q, histogram,
    and ACF.
}

Required packages (in addition to \pkg{bufferedThresholdPanel}):
\pkg{shiny}, \pkg{bslib}, \pkg{plotly}, \pkg{DT}, \pkg{dplyr},
\pkg{tidyr}, \pkg{readr}, \pkg{readxl}, \pkg{gt}, \pkg{shinycssloaders},
\pkg{shinyjs}.  Install them with
\code{install.packages(c("shiny","bslib","plotly","DT","dplyr","tidyr",
  "readr","readxl","gt","shinycssloaders","shinyjs"))}.
}
\examples{
\dontrun{
bptr_shiny()
}
}
\seealso{
\code{\link{bptr}} for the underlying estimation function,
  \code{\link{bptr_table}} for programmatic publication-ready tables.
}
