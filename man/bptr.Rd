% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bptr.R, R/methods.R
\name{bptr}
\alias{bptr}
\alias{print.bptr}
\alias{summary.bptr}
\alias{print.summary.bptr}
\alias{coef.bptr}
\alias{fitted.bptr}
\alias{residuals.bptr}
\alias{nobs.bptr}
\alias{vcov.bptr}
\alias{predict.bptr}
\alias{plot.bptr}
\title{Buffered Panel Threshold Regression (BPTR / PTR)}
\usage{
bptr(
  formula,
  data,
  id,
  time,
  q,
  n_thresh = 1,
  buffer = FALSE,
  trim = 0.15,
  grid_size = 300,
  grid_size_3 = NULL,
  se_type = "HC3",
  ...
)

\method{print}{bptr}(x, digits = 4, ...)

\method{summary}{bptr}(object, ...)

\method{print}{summary.bptr}(x, digits = 4, ...)

\method{coef}{bptr}(object, ...)

\method{fitted}{bptr}(object, ...)

\method{residuals}{bptr}(object, ...)

\method{nobs}{bptr}(object, ...)

\method{vcov}{bptr}(object, ...)

\method{predict}{bptr}(object, newdata = NULL, ...)

\method{plot}{bptr}(x, which = 1:2, ...)
}
\arguments{
\item{formula}{A formula for the model (no intercept needed; fixed effects
are removed by within-demeaning)}

\item{data}{A data frame containing the panel data}

\item{id}{Character string: cross-sectional identifier column}

\item{time}{Character string: time identifier column}

\item{q}{Character string: threshold variable column}

\item{n_thresh}{Integer 1 or 2. Number of threshold parameters (hence 2 or 3
regimes)}

\item{buffer}{Logical. Use buffered (hysteresis) transition (\code{TRUE}) or
standard abrupt transition (\code{FALSE})}

\item{trim}{Numeric trimming fraction for threshold grid (default 0.15)}

\item{grid_size}{Integer. Grid resolution for 1-threshold / 2-regime search
(default 300)}

\item{grid_size_3}{Integer. Grid resolution for 3-regime BTPD 4-D search
(default \code{min(grid_size, 50)}). Increase for finer search at the
cost of computation time}

\item{se_type}{Character. Robust SE type: "HC0", "HC1", "HC2", or "HC3"}

\item{...}{Additional arguments (currently ignored).}

\item{x}{A \code{bptr} object (for \code{print}, \code{plot} and other S3 methods).}

\item{digits}{Number of significant digits (default 4)}

\item{object}{A \code{bptr} object (for \code{summary}, \code{coef},
\code{fitted}, \code{residuals}, \code{nobs}, \code{vcov} and \code{predict}).}

\item{newdata}{An optional data frame for out-of-sample prediction.
Must contain the threshold variable and all regressors.}

\item{which}{Integer vector: \code{1} (residuals vs fitted) and/or
\code{2} (threshold variable with regime boundaries). Default \code{1:2}.}
}
\value{
An object of class \code{"bptr"} — a named list containing:
  \code{gamma}, \code{thresholds}, \code{n_regimes}, \code{n_thresh},
  \code{beta1}, \code{beta2}, \code{beta3} (3-regime only),
  \code{coefficients} (p x K matrix), \code{std_errors} (p x K matrix),
  \code{fitted}, \code{fitted_values}, \code{residuals}, \code{ssr},
  \code{tss_within}, \code{df_residual}, \code{n_obs}, \code{n_groups},
  \code{n_obs_regime}, \code{regime_classification}, plus model metadata.
}
\description{
Estimates one- or two-threshold panel data models with individual fixed effects.
When \code{buffer = FALSE} the classical Hansen (1999) PTR model is estimated.
When \code{buffer = TRUE} the Belarbi et al. (2021) BTPD model with hysteresis
is estimated.

Returns a block-diagonal matrix built from the per-regime sandwich
standard errors. Off-diagonal blocks (cross-regime covariances) are
set to zero.
}
\details{
\strong{Number of regimes and thresholds:}
\itemize{
  \item \code{n_thresh = 1}: 2 regimes, 1 threshold (2 buffer-zone parameters
    for BTPD)
  \item \code{n_thresh = 2}: 3 regimes, 2 thresholds (4 buffer-zone parameters
    for BTPD: rL1, rU1, rL2, rU2 with rL1 <= rU1 < rL2 <= rU2)
}

\strong{Grid search computational note:} For the 3-regime BTPD the search is
over a 4-dimensional parameter space. The grid resolution is automatically
capped at \code{min(grid_size, 50)} for this case to keep computation
feasible (~260 k candidate models at 50 points).  Use \code{grid_size_3} to
override.
}
\examples{
# ── Simulated panel (small for fast execution) ───────────────
set.seed(1)
n <- 20; tt <- 8
df <- data.frame(id=rep(1:n, each=tt), time=rep(1:tt, n),
                 x1=rnorm(n*tt), x2=rnorm(n*tt), q=rnorm(n*tt))
df$y <- 1.5*df$x1 - 0.8*df$x2 +
        (df$q > 0.3) * (-1.2*df$x1 + 0.5*df$x2) + rnorm(n*tt, 0, 0.5)

# 2-regime PTR (abrupt transition)
m1 <- bptr(y ~ x1 + x2, data=df, id="id", time="time", q="q",
           n_thresh=1, grid_size=100)
print(m1)

# 2-regime BTPD (hysteresis buffer zone)
m2 <- bptr(y ~ x1 + x2, data=df, id="id", time="time", q="q",
           n_thresh=1, buffer=TRUE, grid_size=100)
summary(m2)

# ── Built-in panel_data dataset ──────────────────────────────
# Two empirical models follow Hamdi et al. (2025):
#   Model I  — oilRentGDP as threshold, rle as predictor
#   Model II — rle as threshold, oilRentGDP as predictor
\donttest{
data(panel_data)

# Model I: oil dependence threshold
fit_I <- bptr(
  growthRate ~ rle + eci + initialGDP + fdiGDP + capFormGDP +
               inflation + popGrowth + indVAGDP + tradeOpenness,
  data = panel_data, id = "countryId", time = "year",
  q = "oilRentGDP", n_thresh = 1, buffer = TRUE, se_type = "HC3"
)
print(fit_I)

# Model II: rule of law threshold
fit_II <- bptr(
  growthRate ~ oilRentGDP + eci + initialGDP + fdiGDP + capFormGDP +
               inflation + popGrowth + indVAGDP + tradeOpenness,
  data = panel_data, id = "countryId", time = "year",
  q = "rle", n_thresh = 1, buffer = TRUE, se_type = "HC3"
)
print(fit_II)
}

}
